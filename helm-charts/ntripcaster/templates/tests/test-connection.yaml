apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ntripcaster.fullname" . }}-test-connection"
  labels:
    {{- include "ntripcaster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ntripcaster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
