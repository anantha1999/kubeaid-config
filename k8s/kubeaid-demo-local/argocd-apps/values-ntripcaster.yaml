
ingress:
  enabled: true
  className: traefik
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: main.ntrip.test.com
      paths:
        - path: /
          pathType: Prefix
          name: main
          svcPort: 12101
    # - host: relay.ntrip.test.com
    #   paths:
    #     - path: /
    #       pathType: Prefix
    #       name: relay
    #       svcPort: 2101
  tls:
    - hosts:
        - main.ntrip.test.com
      secretName: ntrip-tls